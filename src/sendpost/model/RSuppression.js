/**
 * SendPost API
 * Email API and SMTP relay to not just send and measure email sending, but also alert and optimise. We provide you with tools, expertise and support needed to reliably deliver emails to your customers inboxes on time, every time.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: hello@sendpost.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SuppressionEmail from './SuppressionEmail';

/**
 * The RSuppression model module.
 * @module sendpost/model/RSuppression
 * @version 1.1.2
 */
class RSuppression {
    /**
     * Constructs a new <code>RSuppression</code>.
     * @alias module:sendpost/model/RSuppression
     */
    constructor() { 
        
        RSuppression.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RSuppression</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:sendpost/model/RSuppression} obj Optional instance to populate.
     * @return {module:sendpost/model/RSuppression} The populated <code>RSuppression</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RSuppression();

            if (data.hasOwnProperty('hardBounce')) {
                obj['hardBounce'] = ApiClient.convertToType(data['hardBounce'], [SuppressionEmail]);
            }
            if (data.hasOwnProperty('manual')) {
                obj['manual'] = ApiClient.convertToType(data['manual'], [SuppressionEmail]);
            }
            if (data.hasOwnProperty('spamComplaint')) {
                obj['spamComplaint'] = ApiClient.convertToType(data['spamComplaint'], [SuppressionEmail]);
            }
            if (data.hasOwnProperty('unsubscribe')) {
                obj['unsubscribe'] = ApiClient.convertToType(data['unsubscribe'], [SuppressionEmail]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RSuppression</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RSuppression</code>.
     */
    static validateJSON(data) {
        if (data['hardBounce']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['hardBounce'])) {
                throw new Error("Expected the field `hardBounce` to be an array in the JSON data but got " + data['hardBounce']);
            }
            // validate the optional field `hardBounce` (array)
            for (const item of data['hardBounce']) {
                SuppressionEmail.validateJSON(item);
            };
        }
        if (data['manual']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['manual'])) {
                throw new Error("Expected the field `manual` to be an array in the JSON data but got " + data['manual']);
            }
            // validate the optional field `manual` (array)
            for (const item of data['manual']) {
                SuppressionEmail.validateJSON(item);
            };
        }
        if (data['spamComplaint']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['spamComplaint'])) {
                throw new Error("Expected the field `spamComplaint` to be an array in the JSON data but got " + data['spamComplaint']);
            }
            // validate the optional field `spamComplaint` (array)
            for (const item of data['spamComplaint']) {
                SuppressionEmail.validateJSON(item);
            };
        }
        if (data['unsubscribe']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['unsubscribe'])) {
                throw new Error("Expected the field `unsubscribe` to be an array in the JSON data but got " + data['unsubscribe']);
            }
            // validate the optional field `unsubscribe` (array)
            for (const item of data['unsubscribe']) {
                SuppressionEmail.validateJSON(item);
            };
        }

        return true;
    }

/**
     * @return {Array.<module:sendpost/model/SuppressionEmail>}
     */
    getHardBounce() {
        return this.hardBounce;
    }

    /**
     * @param {Array.<module:sendpost/model/SuppressionEmail>} hardBounce
     */
    setHardBounce(hardBounce) {
        this['hardBounce'] = hardBounce;
    }
/**
     * @return {Array.<module:sendpost/model/SuppressionEmail>}
     */
    getManual() {
        return this.manual;
    }

    /**
     * @param {Array.<module:sendpost/model/SuppressionEmail>} manual
     */
    setManual(manual) {
        this['manual'] = manual;
    }
/**
     * @return {Array.<module:sendpost/model/SuppressionEmail>}
     */
    getSpamComplaint() {
        return this.spamComplaint;
    }

    /**
     * @param {Array.<module:sendpost/model/SuppressionEmail>} spamComplaint
     */
    setSpamComplaint(spamComplaint) {
        this['spamComplaint'] = spamComplaint;
    }
/**
     * @return {Array.<module:sendpost/model/SuppressionEmail>}
     */
    getUnsubscribe() {
        return this.unsubscribe;
    }

    /**
     * @param {Array.<module:sendpost/model/SuppressionEmail>} unsubscribe
     */
    setUnsubscribe(unsubscribe) {
        this['unsubscribe'] = unsubscribe;
    }

}



/**
 * @member {Array.<module:sendpost/model/SuppressionEmail>} hardBounce
 */
RSuppression.prototype['hardBounce'] = undefined;

/**
 * @member {Array.<module:sendpost/model/SuppressionEmail>} manual
 */
RSuppression.prototype['manual'] = undefined;

/**
 * @member {Array.<module:sendpost/model/SuppressionEmail>} spamComplaint
 */
RSuppression.prototype['spamComplaint'] = undefined;

/**
 * @member {Array.<module:sendpost/model/SuppressionEmail>} unsubscribe
 */
RSuppression.prototype['unsubscribe'] = undefined;






export default RSuppression;

